import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bananamilkphone.app',
  appName: '香蕉牛奶机',
  webDir: 'dist',
  androidScheme: 'https'
};

export default config;
