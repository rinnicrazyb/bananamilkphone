/**
 * 默认提取提示词 — 伴侣 AI 场景
 *
 * 基于 LumiMuse 的 EXTRACTION_PROMPT 改编，删除了分类/标签等简化项，
 * 保留时间处理、代词替换、合并规则、输出格式。
 *
 * 用户可在 MemoryPage 中编辑此提示词。
 */
export const DEFAULT_EXTRACTION_PROMPT = `逐条扫描以下对话，提取值得长期记住的信息。输出一个 JSON 对象。

## 已提取的记忆（供参考，避免重复提取相同内容）
{existing_memories}

## 时间处理规则
- 消息中带有时间戳，格式如 "用户 (2026/03/30 02:01):"，可以直接引用这些时间
- 日常闲聊（如晚安、日常问候）：用"某次"泛化，不写具体日期
- 重要互动（约定、纪念日、重要事件）：日期前置，格式为"2026年3月30日，用户+内容"
- 如果无法确定日期，用"某次"但不写空洞内容

## 代词替换规则（重要！）
- 对话中的"AI"、"你"、"助手"以及角色名称 → 在记忆中统一替换为"我"
- 例如："用户喜欢AI的陪伴" → "用户喜欢我的陪伴"（正确）
- 保持第一人称视角

## 合并规则
- 同一个对象/话题/事件的多个信息 → 合并为一条
- 不同对象/话题/事件 → 拆成不同条目
- 同类称呼合并（如"叫我宝宝、宝贝"）
- 每条内容写成适合长期记忆的完整句子

## 要提取的内容
- 用户的称呼方式、爱称
- 用户的喜好、习惯、偏好
- 重要的约定、承诺、纪念日
- 用户的心情、感受、想法
- 共同经历的事件
- 用户的个人信息（不是隐私）

## 不要提取的内容
- "晚安""早安""嗯嗯"等无信息的寒暄
- 客观知识问答
- 纯情绪宣泄无实质信息

## 输出格式
{"memories": [{"content": "记忆内容"}]}

直接输出 JSON 对象，不要代码块标记。顶层只有 memories 字段。

## 对话内容
{conversation_text}

请提取所有值得记住的信息，输出 JSON 对象：`;
